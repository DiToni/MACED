<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anton.ma_ced">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScreenLogin" />
        <activity
            android:name=".Calendar"
            android:label="@string/title_activity_monatuebersicht" />
        <activity
            android:name=".BurgerMenu"
            android:label="@string/title_activity_burger_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OnboardingSite1"
            android:label="@string/title_activity_onboarding_site1"></activity>
    </application>

</manifest>